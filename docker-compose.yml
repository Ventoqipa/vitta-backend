version: '3'
services:
  api:
    image: vitta-api
    build: .
    networks:
      - backend
    ports:
      - "3000:3000"

  web-cli:
    image: vitta-api
    networks:
      - backend
    command: sh

networks:
  backend:
    driver: bridge